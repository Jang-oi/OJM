import React from 'react';
import { Container } from '@mui/material';

const LocationTemplate = ({ element }) => {
    return <Container style={{ marginTop: 50 }}>{element}</Container>;
};

export default LocationTemplate;
